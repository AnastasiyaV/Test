package core3HomeTaskStudent;

import java.util.HashMap;

public class AverageRating {
    static int sumRating = 0;

//    public static int countAverageRating(HashMap<String, Student> studentsStorage) {
//        //double averageRating = ((StudentsWorker.student1.getRating() + StudentsWorker.student2.getRating() + StudentsWorker.student3.getRating()) / studentsStorage.size());
//        for (HashMap<String, Student> currentStudent : studentsStorage) {
//            //sumRating = currentStudent.
//             //.get("rating") + sumRating;
//             //.getRating + sumRating;
//             // sumRating = entry.getValue().getRating + sumRating;
//        }
//        double averageRating = ((sumRating / studentsStorage.size());
//        return (int) Math.round(averageRating);
//    }
}
